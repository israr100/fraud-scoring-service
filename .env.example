# Copy to .env and adjust. All variables are optional (defaults shown).
# Local default is SQLite; point at Postgres for the compose / cloud stack.
# FRAUD_DATABASE_URL=postgresql+psycopg://fraud:fraud@localhost:5432/fraud
# FRAUD_MODEL_PATH=./models/model.joblib
# FRAUD_LOG_PREDICTIONS=true
